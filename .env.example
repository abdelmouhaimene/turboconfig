# Database configuration for docker-compose
DATABASE_USER=turboconfig
DATABASE_PASSWORD=turboconfig_secret
DATABASE_NAME=turboconfig_db

# Redis configuration for docker-compose
REDIS_PASSWORD=redis_secret
